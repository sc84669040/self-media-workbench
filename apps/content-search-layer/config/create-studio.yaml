# Compatibility placeholder. Runtime configuration is centralized in:
# config/local/local.yaml
#
# This file intentionally contains no private paths, tokens, or API keys.
creative_studio_runtime: {}
